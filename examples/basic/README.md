# Basic Example

Hello World with Marten - text responses, JSON, path params, and query params.

## Run

```bash
go run .
```

## Endpoints

- `GET /` - Text response
- `GET /json` - JSON response
- `GET /users/:id` - Path parameters
- `GET /search?q=term&page=1` - Query parameters
